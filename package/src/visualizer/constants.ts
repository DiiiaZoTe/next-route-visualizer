export const ALLOWED_EXTENSIONS = ['.tsx', '.ts', '.js', '.jsx'];
export const NEXT_FILES = [
  'page.',
  'layout.',
  'template.',
  'error.',
  'not-found.',
  'route.',
  'loading.',
];

export const FALLBACK_BASE_URL = 'http://localhost:3000';

export const NODE_SIZE = 160;

export const NODE_SPACING_X = NODE_SIZE;
export const NODE_SPACING_Y = NODE_SIZE;
export const NODE_WIDTH = NODE_SIZE;
export const NODE_HEIGHT = NODE_SIZE;
export const NODE_BORDER = 2;
export const NODE_BORDER_RADIUS = 10;

export const HANDLE_SIZE = 5;